# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

高校生向けプログラミング体験授業「Webページから情報を取り出してみよう」の教材一式。
プログラミングも HTML も未経験の高校生が、Colab で Beautiful Soup を触る **計約90分**。
**どこで区切るかは講師が決める。教材側にコマの区切りを書かない。**

---

## 作業の入口

### ファイルの地図

| 場所 | 中身 | 直していいか |
|---|---|---|
| `index.html` / `items/*.html` | 練習用サイト（30商品） | **✗ 生成物。`tools/build-site.py` を直す** |
| `style.css` | サイトの見た目 | ○ |
| `samples/` | ステップ7で読む Wikipedia の保存版 | 手順は `add-snapshot` スキル |
| `notebooks/student.ipynb` | 受講生用（穴埋め） | ○ |
| `notebooks/instructor.ipynb` | 講師用（解答＋メモ） | ○ |
| `docs/slides.md` | 投影用スライドの原稿（Marp） | ○ |
| `docs/slides.pptx` | 配布用 PowerPoint | **✗ 生成物。原稿を直して再生成** |
| `docs/slides.pdf` | 配布・印刷用 PDF | **✗ 生成物。pptx から再生成** |
| `docs/handout.md` | 受講生用プリント（A4両面2枚） | ○ |
| `docs/lesson-plan.md` | 進行台本 | ○ |
| `docs/deploy.md` | 公開・変換の手順 | ○ |
| `tools/` | 生成とチェックのスクリプト | ○ |
| `README.md` | GitHub の入口（Colab バッジ・公開URL・ステップ表） | ○ **チェッカーが見ていない。手で直す** |

### コマンド

```bash
python3 tools/build-site.py       # DATA → index.html / items/*.html
node tools/slides-to-pptx.mjs     # slides.md → slides.pptx（npm run slides でも同じ）
python3 tools/check-materials.py  # 教材の整合性を31項目チェック（外部ツール不要）
bash tools/render-slides.sh       # pptx を描画して、消えた文字が無いか確認
bash tools/slides-to-pdf.sh       # slides.pptx → slides.pdf（Windows の PowerPoint 経由）
```

`slides-to-pptx.mjs` は入力と出力を引数で差し替えられる（既定は `docs/slides.md` → `docs/slides.pptx`）。
**直す前後の pptx を別名で出して見くらべる**ときはこれを使う。

```bash
node tools/slides-to-pptx.mjs docs/slides.md /tmp/before.pptx
```

必要なものは3系統に分かれていて、揃っていないものは各スクリプトが最初に教えてくれる。

| やること | 要るもの |
|---|---|
| pptx の生成 | Node と `npm install`（`pptxgenjs`） |
| 描画確認（`render-slides.sh`） | Linux 側の `soffice` / `pdftoppm` / Noto CJK |
| PDF 化（`slides-to-pdf.sh`） | Windows 側の PowerPoint（WSL から呼ぶ。詳細は `docs/deploy.md`） |

`build-site.py` と `check-materials.py` は Python だけで動く。外部ツールは要らない。

**何を直しても、最後に `check-materials.py` を通す。**

### スキル

| スキル | 使うとき |
|---|---|
| `edit-site` | 商品・価格・テーマ・商品数を変える |
| `edit-slides` | スライドやプリントを直す |
| `add-snapshot` | 外部サイトを教材に使いたくなった |
| `check-materials` | 何かを直したあと（毎回） |

---

## 1. ゴール

**「動いた」「取れた」の成功体験を最優先。** 文法の網羅的な説明はしない。

**HTML も未習である前提で組む。** タグ・class・入れ子を知らないと「何を探しているのか」が
分からないまま写経になる。授業の冒頭に HTML の話を8分置き、**今回使うタグだけ**を扱う。

## 2. 絶対制約

- **受講生が実行するコードから外部サイトへアクセスしない。** 20人が一斉に叩けば、
  学校の共有グローバルIPからは1つのボットの並列アクセスに見える。
  `student.ipynb` の対象は自前の練習用サイトのみ（`samples/` の保存版を含む）。
  外部サイトを教材にしたいときは `add-snapshot` スキル。
  外部への実アクセスは `instructor.ipynb` の講師実演セルだけに閉じ、その旨をセル内に明記する。
- **環境構築を一切させない。** Colab 標準搭載の `requests` と `bs4` だけで完結。`pip install` を書かない。
- **1セル＝1つのことだけ。** セルを実行するたびに何かが目に見えて変わること。
- **エラーで詰まる箇所を作らない。** 全セルを上から順に実行すれば必ず通る状態を維持。

## 3. 練習用サイトの設計

GitHub Pages のブランチ公開で選べるフォルダは `/(root)` と `/docs` だけなので、
サイトのファイルは**リポジトリ直下**に置く。詳細は `docs/deploy.md`。

テーマは架空の学校購買部「サイバー購買部」。高校生に身近な商品を30点、
5カテゴリ（パン / 軽食 / 飲み物 / 文具 / 日用品）に6点ずつ。
テーマ依存の記述をノートブックに持ちこまない（商品名を決め打ちで書かない）。

### HTML の要件

教材なので、**構造の読みやすさが機能より優先**する。

- インデントを正しく揃える。検証ツールで開いたときに構造が一目で分かること
- class 名は意味が推測できる英単語（`item-name`, `item-price`, `item-stock`）
- 1商品 = 1つの `<div class="item">` で包む。入れ子を深くしない
- 一覧はカテゴリごとに `<section class="category">` で区切り、見出しは `<h3 class="category-name">`。
  **`<h2>` は商品名専用に残す**（`find_all("h2")` の結果がぶれないようにするため）
- カテゴリは一覧側の商品カードにも `<p class="item-category">` として入れる（ステップ3で使う）
- 装飾は `style.css` に外出しし、HTML に `style` 属性を書かない
- 価格は `<span class="item-price">280</span>` のように**数値だけ**を入れる
- 各商品の詳細ページへのリンクを `index.html` から張る（ステップ4で使う）
- 在庫（`item-stock`）は「あり / 残りわずか / 品切れ」の3種。**詳細ページにだけ置く**
- `<title>` と `<h1>` は別の文字列にする（ステップ1のやってみようで差を見せる）
- 商品数は30。価格の**最安・最高は1商品だけ**にして答えを一意にする

見た目は簡素でよいが、購買部のページに見える程度には整える。
生徒がブラウザで開いたときにテンションが下がらないこと。

## 4. ノートブックの設計

| ステップ | 内容 | 新しく学ぶこと |
|---|---|---|
| 0 | ページをブラウザで見る | 相手を知る。コードは書かない |
| 1 | ページのタイトルを取る | `requests` → `BeautifulSoup` → `.title` |
| 2 | 商品名を全部並べる | `find_all()` とループ |
| 3 | 価格も取って最安値を探す | class 指定、数値変換、比較 |
| 4 | ほしいものだけ選ぶ | `if`、`append`、`sort()` |
| 5 | カテゴリごとに数える | 辞書、`get(cat, 0)`、平均 |
| 6 | 詳細ページを辿って在庫を見る | リンクの取得、複数ページの巡回 |
| 7 | 本物の Wikipedia 記事を読む（発展） | `id` 指定、いらない要素の混入、脚注の掃除 |

**教材にコマの区切りを書かない。** 「1コマ目はここまで」「2コマ目のはじめに」のような
セパレーターのスライド・セル・見出しを置かない。区切る場所は講師が当日決める。

ステップ7だけが**発展課題**。進度差の吸収用で、全員が到達する前提で組まない。

ステップ6の巡回は `items[:5]` で**先頭5商品だけ**。30件を1秒間隔で回すと30秒待たされ、
授業が止まる。「ぜんぶやったら何秒かかる？」をマナーの話への橋渡しに使う。

ステップ7で読むのは `samples/` の保存版で、本物の Wikipedia へは出ない。
狙いは「きれいなページ」との対比であって、新しい文法を増やすことではない。

### 各ステップの構造

```
【説明セル(Markdown)】 3〜5行。何をするかだけ。理屈は書かない
【コードセル】         穴埋め or 実行するだけ
【結果を見るセル】     print で結果が出る
【やってみようセル】   1箇所だけ変えて試す
```

### 穴埋めの方針

`student.ipynb` はコードの**一部だけ**を `____` にする。空欄は1セルにつき1〜2箇所まで。

```python
names = soup.find_all("div", class_="____")   # ← ここだけ埋める
```

白紙から書かせない。写経もさせない。「1箇所埋めたら動いた」を繰り返す。

### 文体

- 話し言葉。「〜します」「〜してみよう」
- 専門用語は初出時に1行で言い換える（例：パース＝バラバラに分解して扱いやすくすること）
- 変数名は日本語ローマ字を避け、平易な英単語（`price`, `name`, `items`）

## 5. スライドとプリント

| 教材 | 役割 | 原則 |
|---|---|---|
| `docs/slides.md` | 話の骨格。**説明はここで完結させる** | 1スライド1メッセージ |
| `docs/handout.md` | 手元の早見表 | **暗記させないための道具** |
| `notebooks/student.ipynb` | 手を動かす場所 | 説明は最小限 |

スライドとプリントは**内容が重複してよい**。投影を見逃した生徒が手元で追いつけるようにする。

### スライド

- 構成は「第1部：HTML の話」→「第2部：やってみる」→「マナー」→「発展」
- **第1部で扱うタグは、その日のコードに出てくるものだけ**。網羅しない
  （`title` / `h1` / `h2` / `p` / `div` / `span` / `a` / `sup` と `class` / `id`）
- 山は「商品1つ分の中身」のスライド。**サイトの HTML をそのまま貼る**
- `<span class="item-price">150</span>円` がなぜ分けてあるかを第1部で説明し、
  ステップ3の `int()` の伏線にする
- 1スライドの本文は**5行以内**。表は6行以内。コードは10行以内
- 色は練習用サイトと揃える（`#f0932b` / `#fdf8ef` / `#3a332b`）
- **マナーの話はステップ6のあとに置く。** `time.sleep(1)` で5秒待たされた体感を導入に使う

### プリント

- **A4両面2枚（4ページ）**に収める。増やさない。
  `<div style="page-break-after: always;">` でページの切れ目を明示し、表を分断させない
- **4章・6章・7章・8章の4つだけで早見表として成立する**ように書く
- 穴埋めの**答えは書かない**。ただしタグ表・class 表・命令表は**全部載せる**
- エラー対処表は**症状（画面に出る文字）から引ける**順に並べる

## 6. 進行台本（`docs/lesson-plan.md`）

- タイムテーブル（分単位。開始時刻が積み上がっていること）
- 各ステップで教員が口頭で言うべきこと
- **つまずきポイントと対処**（Colabのログイン、セルの実行順、全角スペース混入、`find` と `find_all` の混同）
- 想定質問と回答例
- スクレイピングのマナー（負荷、利用規約、robots.txt）を5分程度の枠で

マナーの話は「禁止事項の列挙」ではなく、**なぜそうするのかが分かる形**で書く。
技術の使い方を考える題材として位置づける。

## 7. 変更したときの波及先

**片方だけ直すと必ずずれる。** `check-materials.py` が検出する項目もあるが、文章は人が直す。

| 変えたもの | 一緒に直す |
|---|---|
| 商品・価格・カテゴリ | `tools/build-site.py` → 再生成。数を変えたなら `edit-site` スキルの表 |
| 商品数 | 上に加えてノートブック・スライド・プリント・台本・`EXPECTED_ITEMS`・`README.md` |
| ステップの構成（増減・入れ替え） | ノートブック・スライド・プリント・台本・`README.md` のステップ表 |
| class 名 | サイト・ノートブック・スライド6章・プリント6章 |
| `docs/slides.md` | `docs/slides.pptx` と `docs/slides.pdf` を再生成。枚数が変わったら台本の「スライドN〜M」 |
| ステップの所要時間 | 台本のタイムテーブル（通しの1本。00:00 から積み上げ、全体100分以内） |
| 公開URL | ノートブックの `BASE_URL` とステップ0のリンク、台本、`deploy.md`、`README.md`（Colab バッジのリンクも） |
| `samples/` の記事 | ステップ7が前提にする3点（`add-snapshot` スキル） |

## 8. 公開

- リポジトリ https://github.com/yuracode/isshop 、公開URL https://yuracode.github.io/isshop/
- Settings → Pages → Source は `main` / `/(root)`（設定済み）
- URLはノートブック内で**1箇所だけ**（準備セルの `BASE_URL`）。各セルに直書きしない

## 9. 完成の判定

`python3 tools/check-materials.py` が31項目を自動で確認する。**以下は人がやる。**

- [ ] `student.ipynb` を Colab で上から順に実行し、空欄を埋めれば全て動く
- [ ] ステップ1が実行から結果表示まで10秒以内
- [ ] HTML をブラウザの検証ツールで開き、構造が読める
- [ ] 導入からマナーの話まで、通しで90分前後に収まる
- [ ] 投影して、後ろの席から読める。**絵文字が豆腐になっていない**
- [ ] プリントが A4 4ページに収まる
- [ ] 学校のネットワークから公開サイトが開ける／生徒アカウントで Colab が使える

## 10. これまでに決めたこと（同じ議論を繰り返さないために）

| 決めたこと | 理由 |
|---|---|
| 教材に区切りを書かない | 実施形態（50分×2・90分連続・部活動）で切る場所が変わる。**区切りは講師が決める**ので、教材側は通しで書く |
| ステップ4・5（選ぶ・数える）を足した | 90分ぶんの量が要る。取ったあとに**使う**ところまで行くと、スクレイピングが何の役に立つのかが伝わる |
| 日用品の平均285円をわざと見せる | 折りたたみ傘980円が1本混ざっているせい。**平均だけ見ない／中身を見にいく**を体験させる、教材上いちばん学びのある場面 |
| 商品は8→30 | `find_all` のありがたみが出る量。ただし**最安・最高は一意**にして答えを固定 |
| ステップ6は `items[:5]` | 30件×`sleep(1)` = 30秒。授業が止まる。5秒の体感はマナーの話に使える |
| マナーの話はステップ6のあと | 「さっきの5秒」を導入に使うため。前に置くと参照先が無い |
| Wikipedia は保存版を読む | **負荷が理由ではない**（20人は Wikipedia には誤差）。学校の共有IPで並列に見えること、記事が当日書き換わること、マナーの話の直後に外へ出るとメッセージが逆になること |
| Wikipedia は「行儀よくならいい」 | robots.txt に "Friendly, low-speed bots are welcome viewing article pages" とある。ただし `python-requests` の既定 UA では **403 が返る**（実測） |
| `--pptx` に Marp を使わない | 各スライドを画像として貼りこむので PowerPoint で文言を直せない。PDF が要るときだけ Marp |
| スライドは投影向けの配色・サイズ | 教室のスクリーンは色が飛ぶ。地は白寄り（`#fffcf6`）、文字はほぼ黒（`#1a1512`）、見出しは `#8a4a00`。本文 24pt・表 18〜22pt・コード最大 20pt。配色は `slides-to-pptx.mjs` の `C` と `slides.md` の `<style>` の両方にある |
| pptxgenjs は `LAYOUT_WIDE` | `LAYOUT_16x9` は **10×5.625in** で 13.3×7.5in ではない。取り違えると右が切れ、下があふれた要素が消える |
| スライドは描画して確認する | 高さの見積もりだけでは「pptx に文字はあるのに描画されると消える」事故が起きる。`render-slides.sh` が全文突き合わせる |
| HTML は生成物にした | 30ページを手で揃えるのは無理。`build-site.py` の `DATA` が唯一のソース |
| 価格は数値だけ | 「280円」だと文字列処理の説明が要る。体験授業では邪魔 |
| プリントは A4 4ページ | 早見表を削ると暗記させることになる。2ページには収まらなかった |
