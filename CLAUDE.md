# CLAUDE.md

高校生向けプログラミング体験授業用の教材一式を生成するプロジェクト。

## 1. ゴール

プログラミング未経験の高校生が、**Google Colaboratory 上で Beautiful Soup を使い、Webページから情報を取り出す**体験をする。所要 60〜90分。

「動いた」「取れた」の成功体験を最優先。文法の網羅的な説明はしない。

**HTML も未習である前提で組む。** タグ・class・入れ子を知らないと「何を探しているのか」が
分からないまま写経になる。授業の冒頭に HTML の話を8分置き、**今回使うタグだけ**を扱う。

## 2. 成果物

| # | 成果物 | 配置 | 用途 |
|---|---|---|---|
| 1 | 練習用の静的サイト | リポジトリ直下 | GitHub Pages で公開。スクレイピング対象 |
| 2 | 受講生用ノートブック | `notebooks/student.ipynb` | 当日配布。穴埋め形式 |
| 3 | 講師用ノートブック | `notebooks/instructor.ipynb` | 解答＋補足コメント入り |
| 4 | 進行台本 | `docs/lesson-plan.md` | タイムテーブルと想定質問 |
| 5 | 投影用スライド | `docs/slides.md` | Marp 形式。PowerPoint / PDF に変換して投影 |
| 6 | 受講生用プリント | `docs/handout.md` | A4両面2枚。手元に置く早見表 |

## 3. 絶対制約

- **受講生が実行するコードから外部サイトへアクセスしない。** 20人が一斉に叩けば実質的な攻撃になる。
  `student.ipynb` の対象は自前の練習用サイトのみ（`samples/` の保存版を含む）。
  外部サイトを教材にしたい場合は、**事前に1回だけ取得したスナップショットを `samples/` に置く**。
  外部への実アクセスは `instructor.ipynb` の講師実演セルだけに閉じ、その旨をセル内に明記する。
- **環境構築を一切させない。** Colab 標準搭載のライブラリ（`requests`, `bs4`）だけで完結。`pip install` を書かない。
- **1セル＝1つのことだけ。** セルを実行するたびに何かが目に見えて変わること。
- **エラーで詰まる箇所を作らない。** 全セルを上から順に実行すれば必ず通る状態を維持。

## 4. 練習用サイトの設計

### 構成

GitHub Pages のブランチ公開で選べるフォルダは `/(root)` と `/docs` だけなので、
サイトのファイルは**リポジトリ直下**に置く。詳細は `docs/deploy.md`。

```
index.html        商品一覧（トップページ）
items/
├── item01.html   商品詳細
├── item02.html
└── ...           全30商品
style.css
.nojekyll         Jekyll の変換を止める空ファイル
```

### テーマ

初期値は **架空の学校購買部「サイバー購買部」**。高校生に身近な商品を30点、
5カテゴリ（パン / 軽食 / 飲み物 / 文具 / 日用品）に6点ずつ。

テーマ変更は `index.html` / `items/` / `style.css` の書き換えのみで完結すること。ノートブック側にテーマ依存の記述を持ち込まない（商品名を決め打ちで書かない）。

### HTML の要件

教材なので、**構造の読みやすさが機能より優先**する。

- インデントを正しく揃える。ブラウザの検証ツールで開いたときに構造が一目で分かること
- class 名は意味が推測できる英単語にする（`item-name`, `item-price`, `item-stock`）
- 1商品 = 1つの `<div class="item">` で包む。入れ子を深くしない
- 一覧はカテゴリごとに `<section class="category">` で区切る。見出しは `<h3 class="category-name">`。
  `<h2>` は商品名専用に残す（`find_all("h2")` の結果がぶれないようにするため）
- カテゴリは一覧側の商品カードにも `<p class="item-category">` として入れる（ステップ3のやってみようで使う）
- 装飾は `style.css` に外出しし、HTML に `style` 属性を書かない
- 価格は `<span class="item-price">280</span>` のように**数値だけ**を要素に入れる。「280円」にすると文字列処理の話が必要になり、体験授業では邪魔になる
- 各商品の詳細ページへのリンクを `index.html` から張る（ステップ4で使う）
- 在庫状況（`item-stock`）は「あり / 残りわずか / 品切れ」の3種。**詳細ページにだけ置く**（一覧に出すとステップ4の意味がなくなる）
- `<title>` と `<h1>` は別の文字列にする（ステップ1の「やってみよう」で差を見せるため）
- 商品数は30。`find_all` のありがたみが出る量にする。
  1画面に収まらないのは構わないが、価格の**最安・最高は1商品だけ**にして答えを一意にする

見た目は簡素でよいが、「素っ気ないHTML」ではなく購買部のページに見える程度には整える。生徒が対象サイトをブラウザで開いたときに、テンションが下がらないこと。

## 5. ノートブックの設計

### ステップ構成

必ずこの順で、難易度を段階的に上げる。

| ステップ | 内容 | 新しく学ぶこと |
|---|---|---|
| 0 | ページをブラウザで見る | 相手を知る。コードは書かない |
| 1 | ページのタイトルを取る | `requests` → `BeautifulSoup` → `.title` |
| 2 | 商品名を全部並べる | `find_all()` とループ |
| 3 | 価格も取って最安値を探す | class 指定、数値変換、比較 |
| 4 | 詳細ページを辿って在庫を見る | リンクの取得、複数ページの巡回 |
| 5 | 本物の Wikipedia 記事を読む | `id` 指定、いらない要素の混入、脚注の掃除 |

ステップ4は**発展課題**として明示する。進度差の吸収用。全員が到達する前提で組まない。

ステップ4の巡回は `items[:5]` で**先頭5商品だけ**にする。30件を1秒間隔で回すと30秒待たされ、
授業が止まる。「ぜんぶやったら何秒かかる？」という問いかけをマナーの話への橋渡しに使う。

ステップ5も発展課題。90分枠、またはステップ4まで終わった生徒の受け皿。
**読むのは `samples/` の保存版**で、本物の Wikipedia へは出ない。
ここまでの「きれいなページ」との対比（いらない要素が混ざる／脚注 `[1]` が混ざる）が目的で、
新しい文法を増やすことではない。詳細は `samples/README.md`。

### 各ステップの構造

```
【説明セル(Markdown)】 3〜5行。何をするかだけ。理屈は書かない
【コードセル】         穴埋め or 実行するだけ
【結果を見るセル】     print で結果が出る
【やってみようセル】   1箇所だけ変えて試す
```

### 穴埋めの方針

`student.ipynb` では、コードの**一部だけ**を `____` にする。空欄は1セルにつき1〜2箇所まで。

```python
soup = BeautifulSoup(html, "html.parser")
names = soup.find_all("div", class_="____")   # ← ここだけ埋める
```

白紙から書かせない。写経もさせない。「1箇所埋めたら動いた」を繰り返す。

### 文体

- 話し言葉。「〜します」「〜してみよう」
- 専門用語は初出時に1行で言い換える（例：パース＝バラバラに分解して扱いやすくすること）
- 変数名は日本語ローマ字を避け、平易な英単語（`price`, `name`, `items`）

## 6. スライドとプリント

### 役割分担

| 教材 | 役割 | 原則 |
|---|---|---|
| `docs/slides.md` | 話の骨格。**説明はここで完結させる** | 1スライド1メッセージ |
| `docs/handout.md` | 手元の早見表 | **暗記させないための道具** |
| `notebooks/student.ipynb` | 手を動かす場所 | 説明は最小限 |

スライドとプリントは**内容が重複してよい**。投影を見逃した生徒が手元で追いつけるようにする。

### `docs/slides.md`

Marp 形式（`marp: true` の front matter ＋ `---` 区切り）。変換手順は `docs/deploy.md`。

- 構成は「第1部：HTML の話」→「第2部：やってみる」→「マナー」→「発展」
- **第1部で扱うタグは、その日のコードに出てくるものだけ**。網羅しない
  （`title` / `h1` / `h2` / `p` / `div` / `span` / `a` / `sup` と `class` / `id`）
- 山は「商品1つ分の中身」のスライド。**サイトの HTML をそのまま貼る**
- `<span class="item-price">150</span>円` がなぜ分けてあるかを第1部で説明し、
  ステップ3の `int()` の伏線にする
- 1スライドの本文は**5行以内**。表は6行以内。コードは10行以内
- 色は練習用サイトと揃える（`#f0932b` / `#fdf8ef` / `#3a332b`）

**マナーの話はステップ4のあとに置く。** `time.sleep(1)` で5秒待たされた体感を
導入に使うため。ステップ4を省く回のために、代替の入りかたを台本側に用意する。

### `docs/handout.md`

- **A4両面2枚（4ページ）**に収める。増やさない。
  `<div style="page-break-after: always;">` でページの切れ目を明示し、表を分断させない
- **4章・6章・7章・8章の4つだけで早見表として成立する**ように書く
  （1枚に減らしたいときにここだけ印刷できる）
- 穴埋めの**答えは書かない**。ただしタグ表・class 表・命令表は**全部載せる**
  （見ながらやってよい、という前提。写経も暗記もさせないため）
- エラー対処表は**症状（画面に出る文字）から引ける**順に並べる
- 最後にメモ欄と「自分だったら何を取り出したい？」を置く

## 7. `docs/lesson-plan.md`

以下を含める。

- タイムテーブル（分単位。ステップごとの想定所要時間）
- 各ステップで教員が口頭で言うべきこと
- **つまずきポイントと対処**（Colabのログイン、セルの実行順、全角スペース混入、`find` と `find_all` の混同）
- 想定質問と回答例
- スクレイピングのマナーについて話す内容（負荷、利用規約、robots.txt）を5分程度の枠で

マナーの話は「禁止事項の列挙」ではなく、**なぜそうするのかが分かる形**で書く。技術の使い方を考える題材として位置づける。

## 8. 公開手順

練習用サイトを GitHub Pages で公開する。手順は `docs/deploy.md` に記載。

- リポジトリは https://github.com/yuracode/isshop 。公開URLは https://yuracode.github.io/isshop/
- リポジトリの Settings → Pages → Source を `main` ブランチ / `/(root)` に設定
- 公開URLが確定したら、ノートブック冒頭の `BASE_URL` 定数に反映
- URLはノートブック内で**1箇所だけ**に書く。各セルに直書きしない

## 9. 実装の順序

1. 練習用サイトを作る（これが全ての土台。ここが固まらないとノートブックが書けない）
2. `notebooks/instructor.ipynb` を書き、全セルが通ることを確認
3. instructor から空欄を抜いて `student.ipynb` を作る
4. `docs/slides.md`（授業の骨格。ここが決まると台本が書ける）
5. `docs/handout.md`（スライドから早見表を抜き出す）
6. `docs/lesson-plan.md`
7. `docs/deploy.md`

## 10. 完成の判定

- [ ] `student.ipynb` を上から順に実行し、空欄を埋めれば全て動く
- [ ] ステップ1が実行から結果表示まで10秒以内
- [ ] `student.ipynb` から外部サイトへのリクエストが1つもない
      （`grep -c "wikipedia.org\|http" notebooks/student.ipynb` で確認できる）
- [ ] HTML をブラウザの検証ツールで開き、構造が読める
- [ ] ステップ3まででちょうど50分程度の分量（第1部の8分を含む）
- [ ] スライドが PowerPoint に変換でき、絵文字とコードが化けていない
- [ ] プリントが A4両面2枚に収まり、表がページ境界で分断されない
- [ ] スライド・プリント・ノートブックの3つで、class 名の表記がずれていない
- [ ] 最安値・最高値の答えが1商品に定まる
